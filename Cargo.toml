[package]
name = "web-serve"
version = "0.1.0"
authors = ["Mark Thomas <mark.thomas@yoti.com>"]

[dependencies]
postgres = { version = "0.15.2", features = ["with-uuid"] }
regex = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.4.10"
uuid = "0.5"
